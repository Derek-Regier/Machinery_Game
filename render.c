#include "render.h"

void render(const Model *model, UINT8 *base){
  
}
void render_player(const *player, UINT8 *base){

  UINT16 row;
  UINT16 col;
  UINT16 height;

  //pbm??(*base, row, col, *player, height);
  
}
void render_enemy(const *enemy, UINT8 *base){

  UINT16 row;
  UINT16 col;
  UINT16 height;

  //pbm??(*base, row, col, *enemy, height);
  
}
void render_boss(const *boss, UINT8 *base){

  UINT16 row;
  UINT16 col;
  UINT16 height;

  //pbm??(*base, row, col, *boss, height);
  
}
void render_item(const *item, UINT8 *base){

  UINT16 row;
  UINT16 col;
  UINT16 height;

  //pbm??(*base, row, col, *item, height);
  
}
void render_health_bar(const *health_bar, UINT8 *base){

  UINT16 row;
  UINT16 col;
  UINT16 height;

  //pbm??(*base, row, col, *health_bar, height);
  
}
